import { AppContext } from 'next/app'
import React from 'react'

const Article = () => {
  return <div></div>
}

export default Article

export const getStaticPaths = async (ctx: AppContext) => {
  return
}

export const getStaticProps = async () => {
  return
}
